# 컨테이너 내부에서 다시 설치하므로 호스트 node_modules는 컨텍스트에서 제외한다.
node_modules
# 로컬/구버전 빌드 산출물은 이미지에 포함하지 않는다.
dist
# 로컬 전용 빌드 산출물(dist-local)도 이미지에 포함하지 않는다.
dist-local
# 디버그 로그 파일은 이미지 크기 증가만 유발하므로 제외한다.
npm-debug.log*
# 런타임 비밀값(.env)은 이미지에 bake-in 하지 않는다.
.env
