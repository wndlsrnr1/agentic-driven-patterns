interface:
  display_name: "Typescript Foundation Rules"
  short_description: "Language baseline execution skill"
  default_prompt: "Apply typescript-foundation-rules as an embedded policy skill and enforce its workflow and prohibitions on the task."
