interface:
  display_name: "NestJS Security Rules"
  short_description: "Security execution policy rules skill"
  default_prompt: "Apply nestjs-security-rules as an embedded policy skill and enforce its workflow and prohibitions on the task."
